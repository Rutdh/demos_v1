{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Active C++ File (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/single/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "stopOnEntry": false,
            "preLaunchTask": "Build Active C++ File (clang++)"
        }
    ]
}
